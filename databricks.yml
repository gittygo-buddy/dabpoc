# This is a Databricks asset bundle definition for dab_dev.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dab_dev
  uuid: 25da30de-b8b5-44de-9101-a7761111634a

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-550289489940613.13.azuredatabricks.net

  prod:
    mode: production
    workspace:
      host: https://adb-550289489940613.13.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/samanth.tkn@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/samanth.tkn@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: samanth.tkn@gmail.com
        level: CAN_MANAGE
